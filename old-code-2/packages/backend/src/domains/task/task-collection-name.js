const TASK_COLLECTION_NAME_ENUM = Object.freeze({
  task: 'task',
});
const TASK_COLLECTION_NAME_LIST = Object.keys(TASK_COLLECTION_NAME_ENUM);

module.exports = {
  TASK_COLLECTION_NAME_ENUM,
  TASK_COLLECTION_NAME_LIST,
};
