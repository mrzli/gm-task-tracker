const AUTH_COLLECTION_NAME_ENUM = Object.freeze({
  user: 'user',
});
const AUTH_COLLECTION_NAME_LIST = Object.keys(AUTH_COLLECTION_NAME_ENUM);

module.exports = {
  AUTH_COLLECTION_NAME_ENUM,
  AUTH_COLLECTION_NAME_LIST,
};
