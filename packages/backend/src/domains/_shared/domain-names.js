const DOMAIN_NAME_ENUM = Object.freeze({
  auth: 'auth',
  example: 'example',
});
const DOMAIN_NAME_LIST = Object.keys(DOMAIN_NAME_ENUM);

module.exports = {
  DOMAIN_NAME_ENUM,
  DOMAIN_NAME_LIST,
};
